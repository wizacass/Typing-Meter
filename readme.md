## Keyboard Typing Meter

#### How to launch

Program can be launched with 2 arguments using command line. First arguments represents interval duration and second - session duration.

For example:

`go run . 2 5`
